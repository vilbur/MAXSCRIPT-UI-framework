category:	"_Foo-Category"
toolTip:	"tooltip prefix"
buttonText:	"Btn prefix"
(
	isVisible:	true
	isEnabled:	true
	isChecked:	true

	on execute do	(
		print"on execute"
	)
	on AltExecute do	(
		print"on AltExecute"
	)
)
